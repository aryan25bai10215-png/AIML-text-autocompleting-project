# AIML-text-autocompleting-project
This project is a Python-based text autocomplete system that predicts the next word using a bigram model. It learns from user input by updating word frequencies and provides top suggestions through a simple Tkinter GUI, demonstrating basic machine learning concepts without external libraries
